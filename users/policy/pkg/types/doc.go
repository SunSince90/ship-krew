// Package type contains types used by the policy, such as the permissions that
// a user has.
//
// TODO: generally I don't like having a package just for types, but I don't
// really know where to fit these for the moment. So I'll just keep it here
// for now.
package types
